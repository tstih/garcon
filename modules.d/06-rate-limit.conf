# Default development rate limit module.
# Protects the /api/* paths using the same API key header as the header-guard.
# Limits are intentionally generous for development; tune for production use.
path=../modules/libgarcon_rate_limit_module.so
rule=*|/api/*|header:X-Garcon-API-Key|100|60s
